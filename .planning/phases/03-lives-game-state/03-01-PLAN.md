---
phase: 03-lives-game-state
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/systems/lives.js
  - src/config.js
autonomous: true

must_haves:
  truths:
    - "An obstacle passing offscreen decrements the life counter by 1"
    - "Player starts each run with exactly 3 lives"
    - "No mechanism exists to gain extra lives"
    - "Losing all 3 lives emits a GAME_OVER event"
  artifacts:
    - path: "src/systems/lives.js"
      provides: "Life tracking system"
      exports: ["createLives", "resetLives", "getLives"]
    - path: "src/config.js"
      provides: "STARTING_LIVES constant"
      contains: "STARTING_LIVES"
  key_links:
    - from: "src/systems/lives.js"
      to: "events"
      via: "Subscribes to OBSTACLE_MISSED, emits LIFE_LOST and GAME_OVER"
      pattern: "events\\.on\\('OBSTACLE_MISSED'"
---

<objective>
Create the life system that tracks remaining lives, decrements on missed obstacles, and triggers game over when all lives are spent.

Purpose: Without lives the game has no stakes. This plan makes missed obstacles meaningful and establishes the GAME_OVER signal that the state machine (Plan 02) will consume.
Output: A self-contained lives module wired to the event bus, plus a STARTING_LIVES config constant.
</objective>

<execution_context>
@/Users/I529118/.claude/get-shit-done/workflows/execute-plan.md
@/Users/I529118/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/core/events.js
@src/entities/obstacle.js
@src/config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add STARTING_LIVES to config</name>
  <files>src/config.js</files>
  <action>
Add `STARTING_LIVES: 3` to the GAME object in config.js. This is the only source of truth for initial life count. No extra-life mechanic will be added (LOOP-08 forbids it).
  </action>
  <verify>grep 'STARTING_LIVES' src/config.js shows the value 3</verify>
  <done>GAME.STARTING_LIVES === 3 is importable from config.js</done>
</task>

<task type="auto">
  <name>Task 2: Create lives system module</name>
  <files>src/systems/lives.js</files>
  <action>
Create src/systems/lives.js with:

1. Internal `let lives = 0` state variable.
2. `createLives()` — subscribes to OBSTACLE_MISSED on the event bus. On each OBSTACLE_MISSED:
   - Decrement `lives` by 1.
   - Emit `LIFE_LOST` with `{ remaining: lives }`.
   - If `lives <= 0`, emit `GAME_OVER` with `{}`.
3. `resetLives()` — sets `lives` to `GAME.STARTING_LIVES`. Called at game start and on restart.
4. `getLives()` — returns current lives count (for HUD in Phase 4).

Import events from '../core/events.js' and GAME from '../config.js'.

The module must NOT provide any way to increase lives (LOOP-08). Only `resetLives` sets them, only OBSTACLE_MISSED decrements them.
  </action>
  <verify>Read src/systems/lives.js and confirm: subscribes to OBSTACLE_MISSED, emits LIFE_LOST and GAME_OVER, exports createLives/resetLives/getLives, no addLife or gainLife function exists.</verify>
  <done>lives.js exports createLives, resetLives, getLives. On 3 sequential OBSTACLE_MISSED events, GAME_OVER is emitted.</done>
</task>

</tasks>

<verification>
- Import lives.js in a mental trace: after createLives() + resetLives(), emit OBSTACLE_MISSED 3 times via events.emit -> GAME_OVER fires on the 3rd.
- No function in the module increases lives.
- STARTING_LIVES in config is 3.
</verification>

<success_criteria>
- lives.js responds to OBSTACLE_MISSED by decrementing and eventually emitting GAME_OVER
- The module is event-driven (no direct coupling to obstacle.js)
- getLives() returns accurate count at all times
</success_criteria>

<output>
After completion, create `.planning/phases/03-lives-game-state/03-01-SUMMARY.md`
</output>
