---
phase: 06-screens-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/systems/storage.js
  - src/core/state.js
  - src/screens/start.js
  - src/main.js
autonomous: true

must_haves:
  truths:
    - "Opening the game shows a start screen, not immediate gameplay"
    - "Start screen displays personal best from previous sessions"
    - "Pressing any key on start screen begins gameplay"
    - "Personal best updates when score exceeds it"
  artifacts:
    - path: "src/systems/storage.js"
      provides: "localStorage wrapper for personal best persistence"
      exports: ["getPersonalBest", "setPersonalBest"]
    - path: "src/screens/start.js"
      provides: "Start screen renderer with logo, prompt, personal best"
      exports: ["renderStartScreen"]
    - path: "src/core/state.js"
      provides: "MENU state added to state machine"
      contains: "MENU"
  key_links:
    - from: "src/screens/start.js"
      to: "src/systems/storage.js"
      via: "getPersonalBest() call"
      pattern: "getPersonalBest"
    - from: "src/core/state.js"
      to: "events"
      via: "MENU -> PLAYING transition on GAME_START"
      pattern: "GAME_START"
    - from: "src/main.js"
      to: "src/screens/start.js"
      via: "renderStartScreen in render()"
      pattern: "renderStartScreen"
---

<objective>
Add the MENU state to the state machine, create a localStorage persistence module for personal best, and build the start screen that shows a game logo, "Press any key to start" prompt, and personal best score.

Purpose: Players see an inviting entry point rather than being dumped into gameplay. Personal best persistence gives reason to return.
Output: MENU state, storage module, start screen renderer, wiring in main.js
</objective>

<execution_context>
@/Users/I529118/.claude/get-shit-done/workflows/execute-plan.md
@/Users/I529118/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/core/state.js
@src/main.js
@src/config.js
@src/systems/score.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Storage module and MENU state</name>
  <files>src/systems/storage.js, src/core/state.js</files>
  <action>
Create src/systems/storage.js with localStorage wrapper:
- STORAGE_KEY = 'letter-runner-pb' (personal best)
- getPersonalBest(): reads localStorage, returns integer (0 if absent or NaN)
- setPersonalBest(score): writes to localStorage only if score > current best, returns boolean indicating if updated
- Wrap localStorage access in try/catch (private browsing may throw)

Modify src/core/state.js:
- Add MENU to STATES enum: { MENU: 'menu', PLAYING: 'playing', GAME_OVER: 'game_over' }
- Set initial currentState to STATES.MENU (not PLAYING)
- Add GAME_START event listener: transitions from MENU -> PLAYING, emits STATE_CHANGE
- Existing GAME_OVER and GAME_RESTART listeners stay unchanged
- Export a new requestStart() that emits GAME_START (mirrors requestRestart pattern)
  </action>
  <verify>
Run `grep -n "MENU" src/core/state.js` confirms MENU state exists.
Run `node -e "import('./src/systems/storage.js').then(m => console.log(m.getPersonalBest()))"` or just inspect the file for correct export signatures.
  </verify>
  <done>
storage.js exports getPersonalBest/setPersonalBest with localStorage persistence. state.js has MENU state as initial, transitions to PLAYING on GAME_START event.
  </done>
</task>

<task type="auto">
  <name>Task 2: Start screen renderer and main.js wiring</name>
  <files>src/screens/start.js, src/main.js</files>
  <action>
Create src/screens/start.js:
- renderStartScreen(ctx, width, height) function:
  - Fill full canvas with COLORS.BG (import from config)
  - Draw "LETTER RUNNER" title text: bold 42px monospace, white, centered at y=height*0.3
  - Draw tagline "Type to survive" below title: 18px monospace, #888888, centered at y=height*0.3+40
  - Draw "Press any key to start" prompt: 20px monospace, white, centered at y=height*0.65
    - Add a subtle blink effect: only render when Math.floor(Date.now()/600) % 2 === 0
  - Draw personal best: "BEST: {score}" at y=height*0.8, 16px monospace, #ffcc00, centered
    - Import getPersonalBest from storage.js
    - If personal best is 0, show "BEST: ---" instead

Modify src/main.js:
- Import renderStartScreen from './screens/start.js'
- Import { getPersonalBest, setPersonalBest } from './systems/storage.js'
- Import requestStart from state.js (add to existing import)
- Change initial game state: Remove automatic gameplay start. Game should start in MENU.
- In KEY_PRESS handler: if state is MENU, call requestStart() instead of restartGame()
  - Keep existing: if state is GAME_OVER, call restartGame()
- In update(): add early return if state is MENU (no updates in menu)
- In render(): add block at top — if state is MENU, call renderStartScreen(ctx, w, h) and return early (skip game rendering)
- On GAME_OVER event (or STATE_CHANGE to game_over): call setPersonalBest(getScore()) to save high score
  </action>
  <verify>
Run `npx vite build --mode development` to confirm no import/syntax errors.
Open browser: game should show start screen with title, blinking prompt, and "BEST: ---" on first load. Pressing a key starts gameplay. After game over, personal best should display on next start screen.
  </verify>
  <done>
Start screen renders in MENU state showing logo, blinking prompt, and personal best. Any key transitions to PLAYING. Personal best persists via localStorage across sessions.
  </done>
</task>

</tasks>

<verification>
1. Open game fresh: start screen visible with "LETTER RUNNER", blinking prompt, "BEST: ---"
2. Press any key: gameplay starts immediately
3. Play until game over, then press key to restart (goes to PLAYING, not MENU)
4. Reload page: start screen shows with personal best from previous run
5. Clear localStorage: "BEST: ---" shows again
</verification>

<success_criteria>
- MENU state is initial state; gameplay does not auto-start
- Start screen shows logo, tagline, blinking prompt, and personal best
- Personal best persists across browser sessions via localStorage
- Any key on start screen transitions to gameplay
- Game over -> restart still works (does not return to menu)
</success_criteria>

<output>
After completion, create `.planning/phases/06-screens-flow/06-01-SUMMARY.md`
</output>
