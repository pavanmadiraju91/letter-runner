---
phase: 11-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/systems/leaderboard.js
  - src/screens/game-over.js
  - src/main.js
autonomous: true

must_haves:
  truths:
    - "Game-over screen shows only: final score, personal best, delta, and Play Again prompt"
    - "No name entry or initials input exists anywhere in the code"
    - "leaderboard.js is deleted from the filesystem"
    - "No import or require references leaderboard anywhere in src/"
    - "Pressing any key on game-over still restarts instantly"
  artifacts:
    - path: "src/screens/game-over.js"
      provides: "Simplified game-over screen without leaderboard or name entry"
    - path: "src/main.js"
      provides: "No leaderboard imports or references"
---

<objective>
Remove all leaderboard functionality and simplify the game-over screen to show only score, personal best, delta, and a play-again prompt.

Purpose: Simplify the game flow — leaderboard was mocked data anyway. Cleaner game-over experience.
Output: Leaderboard module deleted, game-over screen simplified, restart still works.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Delete leaderboard module and strip references</name>
  <files>src/systems/leaderboard.js, src/screens/game-over.js, src/main.js</files>
  <action>
  1. Delete `src/systems/leaderboard.js` entirely (`rm src/systems/leaderboard.js`)
  
  2. Read `src/screens/game-over.js` and rewrite to remove:
     - All leaderboard imports and references
     - The name entry phase ('name-entry' state)
     - The leaderboard display rendering
     - The dedicated keydown listener for name entry
     - The `isReadyToRestart()` gating logic (no longer needed without name entry)
     
     Keep:
     - Final score display
     - Personal best display  
     - Delta from personal best ("+X" or "NEW BEST!")
     - "Press any key to play again" prompt
     - The dark overlay background
     - Palette colors for styling
  
  3. Read `src/main.js` and remove:
     - Any import of leaderboard module
     - Any initialization of leaderboard
     - Any `isReadyToRestart` check (restart should work immediately on any key in GAME_OVER state)
     
  4. Verify: `grep -r "leaderboard" src/` returns nothing
  5. Verify: `grep -r "initials\|nameEntry\|name-entry\|isReadyToRestart" src/` returns nothing
  </action>
  <verify>
  - `ls src/systems/leaderboard.js 2>/dev/null` returns empty (file deleted)
  - `grep -r "leaderboard" src/` returns nothing
  - `grep -r "initials\|nameEntry" src/` returns nothing
  - `npx vite build` completes without errors
  </verify>
  <done>Leaderboard deleted, game-over simplified to score/PB/delta/restart, build passes.</done>
</task>

</tasks>

<verification>
1. `npx vite build` — zero errors
2. No references to leaderboard in any source file
3. Game-over screen renders cleanly with score info + restart prompt
</verification>

<success_criteria>
- leaderboard.js deleted
- game-over.js simplified (no name entry, no leaderboard display)
- main.js has no leaderboard references
- Restart works on any key in GAME_OVER state
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/11-cleanup/11-01-SUMMARY.md`
</output>
