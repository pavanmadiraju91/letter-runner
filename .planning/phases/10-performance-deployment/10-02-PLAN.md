---
phase: 10-performance-deployment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/deploy.yml
  - package.json
autonomous: true

must_haves:
  truths:
    - "Game is deployed and accessible via a public URL"
    - "Every push to main triggers automatic rebuild and deployment"
    - "Total assets are under 500KB uncompressed"
    - "Game is playable within 2 seconds of page load on a 10Mbps connection"
  artifacts:
    - path: ".github/workflows/deploy.yml"
      provides: "GitHub Actions workflow for GitHub Pages deployment"
      contains: "actions/deploy-pages"
    - path: "package.json"
      provides: "Build script that Vite uses for production output"
      contains: "vite build"
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: "dist/"
      via: "npm run build output"
      pattern: "npm run build"
    - from: "vite.config.js"
      to: "dist/"
      via: "base: './' ensures relative asset paths for GitHub Pages"
      pattern: "base.*\\./"
---

<objective>
Set up GitHub Pages deployment with GitHub Actions CI/CD pipeline.

Purpose: Automate static deployment so the game is publicly accessible via a URL on every push to main, satisfying TECH-10. Also documents that TECH-03 (assets under 500KB) and TECH-04 (load in under 2s) are already satisfied by the current 21KB build.

Output: GitHub Actions workflow file that builds and deploys to GitHub Pages on push to main.
</objective>

<execution_context>
@/Users/I529118/.claude/get-shit-done/workflows/execute-plan.md
@/Users/I529118/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@vite.config.js
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub Actions deployment workflow</name>
  <files>.github/workflows/deploy.yml</files>
  <action>
  Create `.github/workflows/deploy.yml` with the following configuration:

  ```yaml
  name: Deploy to GitHub Pages

  on:
    push:
      branches: [main]
    workflow_dispatch:

  permissions:
    contents: read
    pages: write
    id-token: write

  concurrency:
    group: "pages"
    cancel-in-progress: false

  jobs:
    build:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v4
        - uses: actions/setup-node@v4
          with:
            node-version: 20
            cache: 'npm'
        - run: npm ci
        - run: npm run build
        - uses: actions/upload-pages-artifact@v3
          with:
            path: ./dist

    deploy:
      environment:
        name: github-pages
        url: ${{ pages.url }}
      runs-on: ubuntu-latest
      needs: build
      steps:
        - uses: actions/deploy-pages@v4
          id: pages
  ```

  Key design decisions:
  - Uses the modern GitHub Pages deployment via Actions (not the legacy branch-based approach)
  - `workflow_dispatch` allows manual re-deploys from Actions tab
  - `concurrency` prevents overlapping deployments
  - `npm ci` (not `npm install`) for reproducible builds
  - Node 20 LTS for stability
  - Uploads `./dist` which is Vite's default output directory
  - The `base: './'` in vite.config.js already ensures relative paths work on GitHub Pages subpaths

  NOTE: The repository must have GitHub Pages enabled (Settings -> Pages -> Source: GitHub Actions). This is a one-time manual setting but cannot be automated via this workflow.
  </action>
  <verify>Validate YAML syntax: `cat .github/workflows/deploy.yml | python3 -c "import yaml, sys; yaml.safe_load(sys.stdin.read()); print('Valid YAML')"`. Verify file references correct paths and actions versions.</verify>
  <done>GitHub Actions workflow exists at `.github/workflows/deploy.yml`. It builds with Vite and deploys dist/ to GitHub Pages on push to main. YAML is syntactically valid.</done>
</task>

<task type="auto">
  <name>Task 2: Verify asset budget and load performance (TECH-03, TECH-04)</name>
  <files>package.json</files>
  <action>
  1. Run `npx vite build` and capture output to verify:
     - Total uncompressed size < 500KB (TECH-03)
     - index.html + JS bundle = the only assets (no images, fonts, audio files)
     - Expected: ~20KB JS + ~0.6KB HTML = ~21KB total

  2. Calculate load time for TECH-04:
     - 21KB on 10Mbps = 0.017 seconds transfer time
     - Even with TCP overhead, DNS, TLS: well under 2 seconds
     - No external resources to fetch (everything is procedurally generated)

  3. Add a `"size-check"` script to package.json scripts:
     ```json
     "size-check": "vite build 2>&1 | grep -E '(kB|MB)'"
     ```
     This gives a quick way to verify asset budget hasn't regressed.

  4. Log the verification in the summary: Build output is 20.04kB JS + 0.59kB HTML = 20.63KB total. TECH-03 (< 500KB) passes with 96% headroom. TECH-04 (< 2s on 10Mbps) passes trivially.
  </action>
  <verify>Run `npm run size-check` and confirm output shows total under 500KB. Run `npx vite build` and confirm no new files appeared in dist/ beyond index.html and one JS chunk.</verify>
  <done>Build verified at ~21KB total (well under 500KB limit). Load time on 10Mbps calculated at <0.02s (well under 2s limit). size-check script available for regression monitoring. TECH-03 and TECH-04 formally satisfied.</done>
</task>

</tasks>

<verification>
1. `.github/workflows/deploy.yml` exists with valid YAML syntax
2. Workflow triggers on push to main branch
3. Workflow runs `npm ci && npm run build` then deploys `./dist`
4. `npx vite build` produces output under 500KB
5. `npm run size-check` script works
6. No new dependencies added to package.json (only a new script)
</verification>

<success_criteria>
- TECH-10: GitHub Actions workflow deploys static files to GitHub Pages on every push to main
- TECH-03: Build output verified under 500KB (currently ~21KB, 96% headroom)
- TECH-04: Load time verified under 2 seconds on 10Mbps (currently ~0.02s)
- Deployment pipeline is fully automated (no manual steps after initial Pages enablement)
</success_criteria>

<output>
After completion, create `.planning/phases/10-performance-deployment/10-02-SUMMARY.md`
</output>
