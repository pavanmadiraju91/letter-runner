---
phase: 10-performance-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - src/core/canvas.js
  - src/systems/audio.js
autonomous: true

must_haves:
  truths:
    - "Game works correctly in Chrome, Firefox, Safari, and Edge (latest 2 versions)"
    - "Canvas scales responsively with minimum viewport 800x400px"
    - "AudioContext initializes on first user gesture in all target browsers"
  artifacts:
    - path: "index.html"
      provides: "Minimum viewport meta and responsive constraints"
      contains: "min-width"
    - path: "src/core/canvas.js"
      provides: "Minimum dimension enforcement on resize"
      contains: "Math.max"
    - path: "src/systems/audio.js"
      provides: "Cross-browser AudioContext with Safari fallback"
      contains: "webkitAudioContext"
  key_links:
    - from: "index.html"
      to: "src/core/canvas.js"
      via: "CSS min-width/height + JS enforcement"
      pattern: "min-(width|height)"
    - from: "src/systems/audio.js"
      to: "browser AudioContext"
      via: "webkitAudioContext fallback"
      pattern: "webkitAudioContext"
---

<objective>
Cross-browser compatibility fixes and responsive canvas scaling enforcement.

Purpose: Ensure the game works in all target browsers (Chrome, Firefox, Safari, Edge) and enforces a minimum viewport of 800x400px with graceful scaling, satisfying TECH-05 and TECH-06.

Output: Updated index.html, canvas.js, and audio.js with cross-browser hardening and minimum viewport enforcement.
</objective>

<execution_context>
@/Users/I529118/.claude/get-shit-done/workflows/execute-plan.md
@/Users/I529118/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/core/canvas.js
@src/systems/audio.js
@index.html
@vite.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-browser compatibility hardening</name>
  <files>src/systems/audio.js, src/core/canvas.js, index.html</files>
  <action>
  1. In `src/systems/audio.js`:
     - The `webkitAudioContext` fallback is already present (line 23). Verify it works.
     - Add a safety wrapper around `audioCtx.resume()` for Safari which may reject the promise silently — wrap in `.catch(() => {})` to prevent unhandled rejection.
     - Ensure `OscillatorNode.stop()` calls don't throw in Safari by wrapping in try/catch (Safari throws if oscillator already stopped).

  2. In `src/core/canvas.js`:
     - The `ctx.imageSmoothingEnabled = false` is already set. Add the vendor-prefixed version for older browsers: `ctx.mozImageSmoothingEnabled = false; ctx.webkitImageSmoothingEnabled = false;` (Firefox/Safari legacy).
     - No other canvas changes needed — `getContext('2d')` is universally supported.

  3. In `index.html`:
     - Add `<meta name="color-scheme" content="dark">` in head to prevent white flash on load in all browsers.
     - Add `-webkit-image-rendering: crisp-edges;` as Safari fallback alongside existing `image-rendering` rules (already has `-moz-crisp-edges`).
     - The style block already has `overflow: hidden` which prevents scroll issues.

  4. In `vite.config.js` — NO CHANGES NEEDED. `target: 'es2022'` is supported by all target browsers (Chrome 94+, FF 93+, Safari 16.4+, Edge 94+). The `base: './'` ensures relative paths for any hosting.
  </action>
  <verify>Run `npx vite build` and confirm it builds successfully. Manually review that audio.js has catch on resume(), canvas.js has vendor prefixes, index.html has color-scheme meta.</verify>
  <done>Audio system handles Safari quirks (webkitAudioContext, promise rejection, oscillator stop). Canvas has vendor-prefixed imageSmoothingEnabled. HTML has color-scheme meta and webkit image-rendering fallback. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce minimum viewport 800x400px with responsive scaling</name>
  <files>index.html, src/core/canvas.js</files>
  <action>
  1. In `index.html`, update the `<style>` block:
     - Add `min-width: 800px; min-height: 400px;` to the `html, body` rule.
     - This ensures content has minimum dimensions. On viewports smaller than 800x400, horizontal/vertical scrollbars will appear rather than breaking the game layout.

  2. In `src/core/canvas.js`, update the `resize()` function:
     - After computing `width = window.innerWidth` and `height = window.innerHeight`, enforce minimums:
       ```
       width = Math.max(width, 800);
       height = Math.max(height, 400);
       ```
     - This ensures the logical canvas dimensions never go below 800x400 even if the actual viewport is smaller.
     - The `CANVAS_RESIZE` event will emit the clamped values, so all systems (player position, ground, spawner) use safe dimensions.

  3. The canvas already fills viewport and is DPR-aware (Phase 1). The min-width enforcement ensures no layout breakage on small screens while preserving the full-viewport behavior on normal screens.
  </action>
  <verify>Run `npx vite build`. Open dev tools, resize viewport to 600x300 — canvas should still render at 800x400 (scrollable). Resize to 1920x1080 — canvas fills viewport normally.</verify>
  <done>Canvas enforces minimum 800x400px logical dimensions. HTML body has min-width/min-height. Game remains playable at any viewport size >= 800x400. Smaller viewports get scrollbars rather than broken layout.</done>
</task>

</tasks>

<verification>
1. `npx vite build` completes without errors
2. All source files pass basic syntax check (no console errors on `npx vite dev`)
3. Canvas has minimum dimension enforcement in resize()
4. Audio has Safari-safe promise handling and oscillator stop protection
5. HTML has cross-browser meta tags and CSS fallbacks
</verification>

<success_criteria>
- TECH-05: Code hardened for Chrome, Firefox, Safari, Edge compatibility (AudioContext, canvas rendering, CSS fallbacks)
- TECH-06: Canvas enforces minimum 800x400px with responsive scaling above that threshold
- Build output unchanged in size (no new dependencies)
</success_criteria>

<output>
After completion, create `.planning/phases/10-performance-deployment/10-01-SUMMARY.md`
</output>
