---
phase: 16-theme
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/main.js
  - src/screens/start.js
  - src/screens/game-over.js
  - src/systems/hud.js
  - src/systems/vfx.js
  - src/systems/level-announce.js
  - src/entities/ground.js
  - src/entities/obstacle.js
autonomous: true

must_haves:
  truths:
    - "Start screen adapts to current theme (dark bg + neon title in dark, light bg + muted title in light)"
    - "Game-over screen adapts (dark overlay in dark, light overlay in light)"
    - "HUD text colors (score, level, hearts) adapt to theme"
    - "Obstacles render correctly in both themes"
    - "Ground colors adapt to theme"
    - "Level-announce overlay adapts to theme"
    - "VFX shatter particle colors adapt to theme"
    - "All screens switch reactively when theme changes mid-session"
  artifacts:
    - path: "src/main.js"
      provides: "initTheme() call, dynamic BG fill, getPalette() usage"
      contains: "initTheme"
    - path: "src/screens/start.js"
      provides: "Theme-aware start screen rendering"
      contains: "getPalette"
    - path: "src/screens/game-over.js"
      provides: "Theme-aware game-over rendering"
      contains: "getPalette"
    - path: "src/systems/hud.js"
      provides: "Theme-aware HUD rendering"
      contains: "getPalette"
    - path: "src/entities/obstacle.js"
      provides: "Theme-aware obstacle rendering"
      contains: "getPalette"
    - path: "src/entities/ground.js"
      provides: "Theme-aware ground rendering"
      contains: "getPalette"
    - path: "src/systems/level-announce.js"
      provides: "Theme-aware level announcement"
      contains: "getPalette"
    - path: "src/systems/vfx.js"
      provides: "Theme-aware particle colors"
      contains: "getPalette"
  key_links:
    - from: "src/main.js"
      to: "src/core/theme.js"
      via: "initTheme() call before loop start"
      pattern: "initTheme"
    - from: "src/screens/start.js"
      to: "src/core/theme.js"
      via: "getPalette() + getBG() for colors"
      pattern: "getPalette|getBG"
    - from: "src/entities/obstacle.js"
      to: "src/core/theme.js"
      via: "getPalette() for render colors"
      pattern: "getPalette"
---

<objective>
Wire all rendering code to use the dynamic getPalette() accessor instead of the static COLORS.PALETTE import. Every screen (start, game-over, HUD, gameplay) and every entity (obstacles, ground) reads colors from getPalette() at render time, enabling reactive theme switching without restart.

Purpose: Complete the theme integration so the entire visual layer respects the active theme.
Output: All 8 rendering files updated to use getPalette()/getBG() from theme.js.
</objective>

<execution_context>
@/Users/I529118/.claude/get-shit-done/workflows/execute-plan.md
@/Users/I529118/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-theme/16-01-SUMMARY.md
@src/core/theme.js
@src/config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire main.js and screen renderers to theme</name>
  <files>src/main.js, src/screens/start.js, src/screens/game-over.js</files>
  <action>
1. Update src/main.js:
   - Add import: `import { initTheme, getPalette, getBG } from './core/theme.js';`
   - Call `initTheme();` BEFORE `initCanvas();` (so body bg is set before canvas measures).
   - In the render() function, replace `ctx.fillStyle = COLORS.BG;` with `ctx.fillStyle = getBG();`
   - Replace all `COLORS.PALETTE.X` references with `getPalette().X`:
     - `COLORS.PALETTE.WHITE` → `getPalette().WHITE`
     - `COLORS.PALETTE.DIM` → `getPalette().DIM`
     - `COLORS.PALETTE.MAGENTA` → `getPalette().MAGENTA`
   - Remove the `COLORS` import if no longer needed (keep if GAME still imported from config).
     Actually keep importing COLORS for COLORS.BG reference in getBG — but it's handled by theme.js now.
     Just keep `import { GAME } from './config.js';` (drop COLORS from the import).

2. Update src/screens/start.js:
   - Replace `import { COLORS } from '../config.js';` with `import { getPalette, getBG } from '../core/theme.js';`
   - In renderStartScreen():
     - `COLORS.BG` → `getBG()`
     - `COLORS.PALETTE.CYAN` → `getPalette().CYAN`
     - `COLORS.PALETTE.MID` → `getPalette().MID`
     - `COLORS.PALETTE.WHITE` → `getPalette().WHITE`
     - `COLORS.PALETTE.BEST_TEXT` → `getPalette().BEST_TEXT`
   - For shadowColor in light mode, the glow should be subtler. Use getPalette().CYAN (it's already muted in PALETTE_LIGHT).

3. Update src/screens/game-over.js:
   - Replace `import { COLORS } from '../config.js';` with `import { getPalette } from '../core/theme.js';`
   - In renderGameOverScreen():
     - `COLORS.PALETTE.PANEL` → `getPalette().PANEL`
     - `COLORS.PALETTE.MAGENTA` → `getPalette().MAGENTA`
     - `COLORS.PALETTE.WHITE` → `getPalette().WHITE`
     - `COLORS.PALETTE.BEST_TEXT` → `getPalette().BEST_TEXT`
     - `COLORS.PALETTE.GREEN` → `getPalette().GREEN`
  </action>
  <verify>
  - `grep "initTheme" src/main.js` confirms initialization
  - `grep "getPalette" src/screens/start.js` confirms theme usage
  - `grep "getPalette" src/screens/game-over.js` confirms theme usage
  - `grep -c "COLORS.PALETTE" src/main.js src/screens/start.js src/screens/game-over.js` returns 0 for all three
  </verify>
  <done>main.js calls initTheme() on startup and uses getBG() for canvas clear. Start screen and game-over screen render using getPalette() with zero remaining COLORS.PALETTE references.</done>
</task>

<task type="auto">
  <name>Task 2: Wire HUD, ground, obstacles, VFX, and level-announce to theme</name>
  <files>src/systems/hud.js, src/entities/ground.js, src/entities/obstacle.js, src/systems/vfx.js, src/systems/level-announce.js</files>
  <action>
1. Update src/systems/hud.js:
   - Replace `import { GAME, COLORS } from '../config.js';` with `import { GAME } from '../config.js';` and add `import { getPalette } from '../core/theme.js';`
   - In renderHUD():
     - `COLORS.PALETTE.SCORE_TEXT` → `getPalette().SCORE_TEXT`
     - `COLORS.PALETTE.LEVEL_TEXT` → `getPalette().LEVEL_TEXT`
     - `COLORS.PALETTE.HEART_FULL` → `getPalette().HEART_FULL`
     - `COLORS.PALETTE.HEART_EMPTY` → `getPalette().HEART_EMPTY`

2. Update src/entities/ground.js:
   - Replace `import { GAME, COLORS } from '../config.js';` with `import { GAME } from '../config.js';` and add `import { getPalette } from '../core/theme.js';`
   - In renderGround():
     - `COLORS.PALETTE.GROUND_BASE` → `getPalette().GROUND_BASE`
     - `COLORS.PALETTE.GROUND_LINE` → `getPalette().GROUND_LINE`
     - `COLORS.PALETTE.CYAN` → `getPalette().CYAN`

3. Update src/entities/obstacle.js:
   - Replace `import { GAME, COLORS, COMBO, OBSTACLE_VFX } from '../config.js';` with `import { GAME, COMBO, OBSTACLE_VFX } from '../config.js';` and add `import { getPalette } from '../core/theme.js';`
   - In renderSingleObstacle() and renderComboObstacle(), replace all `COLORS.PALETTE.X` with `getPalette().X`:
     - OBSTACLE_GLOW, OBSTACLE_BODY, OBSTACLE_BORDER, OBSTACLE_LETTER, DIM, GREEN, YELLOW, MAGENTA
   - Call `const P = getPalette();` at the top of each render function for cleaner code, then use `P.OBSTACLE_GLOW` etc.

4. Update src/systems/vfx.js:
   - Replace `import { COLORS } from '../config.js';` with `import { getPalette } from '../core/theme.js';`
   - The SHATTER_COLORS array is currently static. Since particles spawn at a point in time with a fixed color, make spawnDestroyParticles() call getPalette() at spawn time:
     ```js
     function spawnDestroyParticles(x, y) {
       if (!canSpawnParticles()) return;
       const P = getPalette();
       const colors = [P.MAGENTA, P.CYAN, P.YELLOW];
       for (let i = 0; i < colors.length; i++) {
         spawnParticles(x, y, 3, { ...SHATTER_CONFIG, color: colors[i] });
       }
     }
     ```
   - Remove the module-level SHATTER_COLORS array.

5. Update src/systems/level-announce.js:
   - Replace `import { COLORS } from '../config.js';` with `import { getPalette } from '../core/theme.js';`
   - In renderLevelAnnounce():
     - `COLORS.PALETTE.WHITE` → `getPalette().WHITE`
     - `COLORS.PALETTE.CYAN` → `getPalette().CYAN`
  </action>
  <verify>
  - `grep -rc "COLORS.PALETTE" src/systems/hud.js src/entities/ground.js src/entities/obstacle.js src/systems/vfx.js src/systems/level-announce.js` returns 0 for all
  - `grep "getPalette" src/systems/hud.js` confirms usage
  - `grep "getPalette" src/entities/obstacle.js` confirms usage
  - Game runs without errors in both dark and light system modes
  - Toggle system preference: all gameplay elements (obstacles, ground, HUD, level announce, particles) switch colors reactively
  </verify>
  <done>All rendering files use getPalette() for colors. Zero remaining COLORS.PALETTE references in the codebase. Game visuals adapt completely to both dark and light system preferences, switching reactively mid-session.</done>
</task>

</tasks>

<verification>
1. Dark mode: game looks identical to before Phase 16 (no visual regression)
2. Light mode: all text is dark on light background, accents are muted, bg-light.png shows
3. Start screen: title, tagline, prompt, and best score all use theme-appropriate colors
4. Gameplay: obstacles, ground, HUD, level-announce, particles all use theme colors
5. Game-over: overlay, score, best, delta, prompt all adapt to theme
6. Mid-session toggle: every visual element switches immediately without restart
7. No console errors or rendering artifacts in either mode
8. `grep -r "COLORS.PALETTE" src/` returns zero matches (complete migration)
</verification>

<success_criteria>
- Zero remaining COLORS.PALETTE references in src/ (fully migrated to getPalette())
- Dark mode is visually identical to pre-theme game (regression-free)
- Light mode renders with muted accents and dark-on-light text across all screens
- Toggling system preference mid-session triggers immediate visual switch on all elements
- All 5 success criteria from the phase definition pass
</success_criteria>

<output>
After completion, create `.planning/phases/16-theme/16-02-SUMMARY.md`
</output>
