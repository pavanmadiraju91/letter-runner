---
phase: 08-particles-juice
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/systems/vfx.js
  - src/systems/hud.js
  - src/main.js
autonomous: true

must_haves:
  truths:
    - "Losing a life causes a brief red flash over the entire screen for ~200ms"
    - "When a life is lost, the corresponding heart icon dims (already shows empty color)"
    - "Red flash does not stack or flicker on rapid life loss"
  artifacts:
    - path: "src/systems/vfx.js"
      provides: "Screen flash state management (red flash on LIFE_LOST)"
      exports: ["getScreenFlash"]
  key_links:
    - from: "src/systems/vfx.js"
      to: "src/core/events.js"
      via: "listens to LIFE_LOST event for red screen flash"
      pattern: "events.on.*LIFE_LOST"
    - from: "src/main.js"
      to: "src/systems/vfx.js"
      via: "render draws red overlay when getScreenFlash returns active state"
      pattern: "getScreenFlash"
---

<objective>
Implement damage feedback: red screen flash on life lost (VFX-06) and verify heart dim (VFX-07).

Purpose: When the player loses a life, two visual signals communicate damage — a brief red screen flash creates urgency, and the heart icon dimming shows the permanent cost. These negative feedback signals balance the positive particle effects from Plan 02.

Output: Extended vfx.js with screen flash state, main.js renders red overlay, and heart dimming verified (already implemented in HUD via HEART_EMPTY color).
</objective>

<execution_context>
@/Users/I529118/.claude/get-shit-done/workflows/execute-plan.md
@/Users/I529118/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@src/systems/vfx.js
@src/systems/hud.js
@src/systems/lives.js
@src/main.js
@src/config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add red screen flash to VFX module</name>
  <files>src/systems/vfx.js</files>
  <action>
Extend src/systems/vfx.js with screen flash state:

Add module-level state:
- `screenFlashTimer = 0`
- `const SCREEN_FLASH_DURATION = 0.2` — 200ms red flash (VFX-06)

In `createVFX()`, add event subscription:
- `LIFE_LOST`: set screenFlashTimer = SCREEN_FLASH_DURATION (note: if timer is already > 0, just reset it — prevents stacking/flickering)

Also subscribe to `GAME_RESTART`: reset screenFlashTimer = 0

In `updateVFX(dt)`:
- Add: if (screenFlashTimer > 0) screenFlashTimer = Math.max(0, screenFlashTimer - dt);

Add new export:
- `getScreenFlash()` — returns { active: screenFlashTimer > 0, alpha: screenFlashTimer / SCREEN_FLASH_DURATION }. The alpha allows the flash to fade out rather than pop off abruptly.

Note: Check what event the lives system emits when a life is lost. From the codebase context, when an obstacle is missed, OBSTACLE_MISSED is emitted, and lives.js decrements. The lives system likely emits LIFE_LOST. Let me verify — if it doesn't exist, listen to OBSTACLE_MISSED instead.

Actually, looking at the existing event flow: matcher.js emits OBSTACLE_DESTROYED on success. For misses, obstacle.js or the cleanup function emits OBSTACLE_MISSED. Lives.js subscribes to that. The VFX module should listen to the same event that triggers life loss.

Strategy: Listen to OBSTACLE_MISSED (the event that causes life decrement) rather than hoping a LIFE_LOST event exists. This is the source-of-truth event for "player took damage."

Update the subscription to: `events.on('OBSTACLE_MISSED', () => { screenFlashTimer = SCREEN_FLASH_DURATION; });`
  </action>
  <verify>File exports getScreenFlash. Build succeeds. `grep -c "SCREEN_FLASH" src/systems/vfx.js` returns >= 2.</verify>
  <done>VFX module tracks screen flash state, triggered by OBSTACLE_MISSED, with 200ms duration and fading alpha</done>
</task>

<task type="auto">
  <name>Task 2: Render red screen flash and verify heart dimming</name>
  <files>src/main.js</files>
  <action>
In src/main.js:

1. Update VFX import to include getScreenFlash: `import { createVFX, updateVFX, getPlayerFlash, getScreenFlash } from './systems/vfx.js';`

2. In render(), AFTER renderHUD and renderLevelAnnounce, but BEFORE the Game Over screen check, add the red flash overlay:

```js
// VFX-06: Red screen flash on life lost
const flash = getScreenFlash();
if (flash.active) {
  ctx.save();
  ctx.globalAlpha = flash.alpha * 0.3; // max 30% opacity — noticeable but not blinding
  ctx.fillStyle = COLORS.PALETTE.MAGENTA; // using magenta (red-pink) from palette, not pure red
  ctx.fillRect(0, 0, w, h);
  ctx.restore();
}
```

Note on VFX-07 (heart dim): This is ALREADY IMPLEMENTED in hud.js! The renderHUD function already draws hearts with HEART_FULL color for remaining lives and HEART_EMPTY color for lost ones. When lives decrement (on OBSTACLE_MISSED via lives.js), the next render frame draws fewer full hearts. No additional work needed — VFX-07 is satisfied by existing code.

Verify by reading hud.js line where it checks `i < remaining` and draws HEART_EMPTY for lost hearts.
  </action>
  <verify>
1. Run game, let an obstacle pass the player
2. Observe: brief red/magenta flash covers screen for ~200ms
3. Observe: one heart icon changes from full (magenta) to dim (dark gray)
4. Let another pass: another flash, another heart dims
5. Build succeeds: `npx vite build --mode production`
6. Confirm VFX-07: `grep "HEART_EMPTY" src/systems/hud.js` shows it's used for lost lives
  </verify>
  <done>Red screen flash renders on life lost (VFX-06, ~200ms magenta overlay). Heart dimming confirmed working (VFX-07, HEART_EMPTY color for lost hearts in existing HUD code).</done>
</task>

</tasks>

<verification>
1. Let obstacle pass player → red/magenta flash covers screen for ~200ms then fades
2. Heart icon changes from HEART_FULL to HEART_EMPTY color on that same event
3. Multiple rapid losses don't cause flicker (timer resets, doesn't stack)
4. Flash renders above game content but below Game Over overlay
5. Build succeeds without errors
6. VFX-07 verified via existing hud.js HEART_FULL/HEART_EMPTY logic (no new code needed)
</verification>

<success_criteria>
- Losing a life produces visible red/magenta screen flash for ~200ms (VFX-06)
- Heart icons dim when lives are lost (VFX-07 — already implemented, verified)
- Flash fades out smoothly (alpha decay, not abrupt cutoff)
- No flash stacking on rapid life loss
- All Phase 8 requirements covered: VFX-04 through VFX-10 (with VFX-08 from Phase 6)
</success_criteria>

<output>
After completion, create `.planning/phases/08-particles-juice/08-03-SUMMARY.md`
</output>
