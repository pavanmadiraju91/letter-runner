---
phase: 07-visual-style
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/entities/ground.js
  - src/screens/start.js
  - src/screens/game-over.js
  - src/systems/hud.js
  - src/systems/level-announce.js
autonomous: true

must_haves:
  truths:
    - "Start screen uses neon palette colours (cyan title, dim tagline, yellow best score)"
    - "Game over screen uses neon palette colours consistently with start screen"
    - "HUD text uses palette tokens instead of inline hex strings"
    - "Ground band uses palette colours"
    - "Level-up announcement uses palette colours"
    - "All screens feel visually cohesive — same colour language throughout"
  artifacts:
    - path: "src/screens/start.js"
      provides: "Start screen with neon palette styling"
      contains: "PALETTE"
    - path: "src/screens/game-over.js"
      provides: "Game over screen with neon palette styling"
      contains: "PALETTE"
    - path: "src/systems/hud.js"
      provides: "HUD with palette token colours"
      contains: "PALETTE"
    - path: "src/entities/ground.js"
      provides: "Ground with palette colours"
      contains: "PALETTE"
    - path: "src/systems/level-announce.js"
      provides: "Level announce with palette colours"
      contains: "PALETTE"
  key_links:
    - from: "src/screens/start.js"
      to: "src/config.js"
      via: "import COLORS"
      pattern: "COLORS\\.PALETTE"
    - from: "src/screens/game-over.js"
      to: "src/config.js"
      via: "import COLORS"
      pattern: "COLORS\\.PALETTE"
    - from: "src/systems/hud.js"
      to: "src/config.js"
      via: "import COLORS"
      pattern: "COLORS\\.PALETTE"
---

<objective>
Apply the neon-on-dark palette to all remaining visual surfaces: screens, HUD, ground, and overlays.

Purpose: Ensures every part of the game uses the centralized palette for a cohesive pixel-art aesthetic. After this plan, no inline hex colours remain in rendering code — everything references COLORS.PALETTE tokens.

Output: All 5 rendering files updated to use palette tokens. The game reads as intentional pixel art with a unified neon-on-dark look across all states.
</objective>

<execution_context>
@/Users/I529118/.claude/get-shit-done/workflows/execute-plan.md
@/Users/I529118/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-visual-style/07-01-SUMMARY.md
@src/config.js
@src/entities/ground.js
@src/screens/start.js
@src/screens/game-over.js
@src/systems/hud.js
@src/systems/level-announce.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply palette to ground, HUD, and level-announce</name>
  <files>src/entities/ground.js, src/systems/hud.js, src/systems/level-announce.js</files>
  <action>
**ground.js:**
- Import `COLORS` from `'../config.js'` (already imports GAME, add COLORS)
- Replace `'#1a1a2e'` with `COLORS.PALETTE.GROUND_BASE`
- Replace `'#2a2a4e'` with `COLORS.PALETTE.GROUND_LINE`
- Add a subtle top-edge line: after the ground band fill, draw a 1px line at `ground.y` using `COLORS.PALETTE.CYAN` at 30% opacity (`ctx.globalAlpha = 0.3` then reset). This gives a neon horizon line.

**hud.js:**
- Import `COLORS` from `'../config.js'` (already imports GAME, add COLORS)
- Replace score `'#ffffff'` with `COLORS.PALETTE.SCORE_TEXT`
- Replace level text colour: use `COLORS.PALETTE.LEVEL_TEXT` (cyan) for the level indicator
- Replace heart full `'#ff3366'` with `COLORS.PALETTE.HEART_FULL`
- Replace heart empty `'#2a2a3a'` with `COLORS.PALETTE.HEART_EMPTY`

**level-announce.js:**
- Import `COLORS` from `'../config.js'`
- Replace text fill `'#ffffff'` with `COLORS.PALETTE.WHITE`
- Replace shadowColor `'#00ffcc'` with `COLORS.PALETTE.CYAN`
- Increase shadowBlur from 20 to 30 for stronger neon glow effect
  </action>
  <verify>Run `npx vite build` — no errors. Open dev server, confirm: ground has cyan top-edge line, HUD level text is cyan, hearts use correct colours, level-up text has strong glow.</verify>
  <done>ground.js, hud.js, and level-announce.js all reference COLORS.PALETTE tokens. No inline hex colours in render functions.</done>
</task>

<task type="auto">
  <name>Task 2: Apply palette to start screen and game-over screen</name>
  <files>src/screens/start.js, src/screens/game-over.js</files>
  <action>
**start.js:**
- Already imports COLORS — use it fully:
- Title `'#ffffff'` → `COLORS.PALETTE.CYAN` (make title pop in neon cyan)
- Tagline `'#888888'` → `COLORS.PALETTE.MID`
- Blink prompt `'#ffffff'` → `COLORS.PALETTE.WHITE`
- Best score `'#ffcc00'` → `COLORS.PALETTE.BEST_TEXT`
- Add a subtle glow to the title: `ctx.shadowColor = COLORS.PALETTE.CYAN; ctx.shadowBlur = 15;` before title fillText, then reset `ctx.shadowBlur = 0` after.

**game-over.js:**
- Import `COLORS` from `'../config.js'`
- Dark overlay `'rgba(0, 0, 0, 0.85)'` → `COLORS.PALETTE.PANEL`
- "GAME OVER" title `'#ffffff'` → `COLORS.PALETTE.MAGENTA` (use magenta for drama, add glow: shadowColor MAGENTA, shadowBlur 15)
- Score text `'bold 28px'` → keep white `COLORS.PALETTE.WHITE`
- Best text `'#ffcc00'` → `COLORS.PALETTE.BEST_TEXT`
- New best `'#00ff88'` → `COLORS.PALETTE.GREEN`
- Negative delta `'#ff4466'` → `COLORS.PALETTE.MAGENTA`
- Leaderboard header `'#aaaaaa'` → `COLORS.PALETTE.MID`
- Leaderboard entries `'#cccccc'` → `COLORS.PALETTE.LIGHT`
- Player highlight `'#00ffcc'` → `COLORS.PALETTE.CYAN`
- Name entry text `'#ffffff'` → `COLORS.PALETTE.WHITE`
- Hint text `'#888888'` → `COLORS.PALETTE.MID`
- Play again blink `'#ffffff'` → `COLORS.PALETTE.WHITE`

All shadow effects: reset `ctx.shadowBlur = 0` after each glow usage to prevent bleeding into subsequent draws.
  </action>
  <verify>Run `npx vite build` — no errors. Open dev server: start screen title glows cyan, game-over title glows magenta, all text uses consistent palette colours, no raw hex strings remain in render paths.</verify>
  <done>Both screen files reference COLORS.PALETTE exclusively. Start screen has glowing cyan title. Game-over screen has glowing magenta title. Visual language is cohesive across all game states.</done>
</task>

</tasks>

<verification>
1. `npx vite build` completes without errors
2. Start screen: cyan glowing title, dim tagline, yellow best score
3. Game over screen: magenta glowing "GAME OVER", consistent colours
4. HUD: white score, cyan level, correctly-coloured hearts
5. Ground: dark band with cyan horizon line
6. Level-up announce: white text with strong cyan glow
7. Grep all render files for inline hex — no orphaned colour strings remain (all use PALETTE tokens)
</verification>

<success_criteria>
- All 5 files import and use COLORS.PALETTE tokens
- No inline hex colour strings remain in rendering functions (except BG which stays in COLORS root)
- Visual cohesion: all screens share the same neon colour language
- The game reads as intentional pixel art with a unified aesthetic
</success_criteria>

<output>
After completion, create `.planning/phases/07-visual-style/07-02-SUMMARY.md`
</output>
