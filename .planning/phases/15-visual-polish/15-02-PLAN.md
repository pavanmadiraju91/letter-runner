---
phase: 15-visual-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/entities/obstacle.js
  - src/config.js
autonomous: true

must_haves:
  truths:
    - "Obstacle letters are large, bold, and readable at any speed"
    - "Obstacles pulse/glow when inside the danger zone"
    - "No shadowBlur calls remain in obstacle rendering (performance)"
  artifacts:
    - path: "src/entities/obstacle.js"
      provides: "Screen-blend glow, danger-zone pulse, larger outlined letters"
      contains: "globalCompositeOperation"
  key_links:
    - from: "src/entities/obstacle.js"
      to: "src/config.js"
      via: "GAME.DANGER_ZONE_START import"
      pattern: "DANGER_ZONE_START"
---

<objective>
Replace shadowBlur with performant screen-blend glow, add danger-zone pulsing effect to obstacles, and increase letter size with dark outlines for readability.

Purpose: VIS-03 (danger zone pulse), VIS-04 (screen-blend glow), VIS-05 (letter readability) — obstacles currently use expensive shadowBlur for glow and letters are small. This plan fixes performance and readability in one pass since all changes are in obstacle.js.

Output: Obstacles glow via globalCompositeOperation='lighter' instead of shadowBlur, pulse when in danger zone, and display 26px bold letters with dark outlines.
</objective>

<execution_context>
@/Users/I529118/.claude/get-shit-done/workflows/execute-plan.md
@/Users/I529118/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/entities/obstacle.js
@src/config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace shadowBlur with screen-blend glow and add danger-zone pulse</name>
  <files>src/entities/obstacle.js, src/config.js</files>
  <action>
Rewrite the glow/rendering approach in obstacle.js:

1. Remove ALL shadowBlur and shadowColor usage from both renderSingleObstacle and renderComboObstacle. No ctx.shadowBlur, no ctx.shadowColor anywhere in the file.

2. Add a helper function at top of obstacle.js to determine if obstacle is in danger zone:
   ```
   import { getWidth } from '../core/canvas.js';
   
   function isInDangerZone(obs) {
     return obs.x <= getWidth() * GAME.DANGER_ZONE_START;
   }
   ```

3. Replace glow with screen-blend technique in renderSingleObstacle:
   ```
   function renderSingleObstacle(ctx, obs) {
     const x = Math.round(obs.x);
     const y = Math.round(obs.y);
     const w = obs.width;
     const h = obs.height;
     const inDanger = isInDangerZone(obs);
   
     // Screen-blend glow (replaces shadowBlur) — VIS-04
     if (inDanger) {
       // Pulsing glow intensity when in danger zone — VIS-03
       const pulse = 0.4 + 0.4 * Math.sin(Date.now() * 0.01);
       ctx.save();
       ctx.globalCompositeOperation = 'lighter';
       ctx.globalAlpha = pulse;
       ctx.fillStyle = COLORS.PALETTE.OBSTACLE_GLOW;
       ctx.fillRect(x - 3, y - 3, w + 6, h + 6);
       ctx.restore();
     }
   
     // Body fill
     ctx.fillStyle = COLORS.PALETTE.OBSTACLE_BODY;
     ctx.fillRect(x, y, w, h);
   
     // Neon border (2px inset)
     ctx.strokeStyle = COLORS.PALETTE.OBSTACLE_BORDER;
     ctx.lineWidth = 2;
     ctx.strokeRect(x + 1, y + 1, w - 2, h - 2);
   
     // Letter — VIS-05: 26px bold with dark outline for readability
     ctx.font = 'bold 26px monospace';
     ctx.textAlign = 'center';
     ctx.textBaseline = 'middle';
     // Dark outline (stroke first, then fill)
     ctx.strokeStyle = '#000000';
     ctx.lineWidth = 3;
     ctx.strokeText(obs.letter, x + w / 2, y + h / 2);
     ctx.fillStyle = COLORS.PALETTE.OBSTACLE_LETTER;
     ctx.fillText(obs.letter, x + w / 2, y + h / 2);
   }
   ```

4. Apply same pattern to renderComboObstacle:
   - Remove shadowBlur/shadowColor lines
   - Add screen-blend danger zone glow around the full combo box (same pattern as single)
   - Increase letter font from 'bold 20px monospace' to 'bold 26px monospace'
   - Add dark outline to each letter (ctx.strokeStyle = '#000000', ctx.lineWidth = 3, ctx.strokeText before ctx.fillText)

5. Remove the final shadow reset at end of renderObstacles (no longer needed since we don't use shadow):
   ```
   // Remove these lines:
   ctx.shadowColor = 'transparent';
   ctx.shadowBlur = 0;
   ```

6. Add OBSTACLE config to src/config.js (optional, for tuning):
   ```
   export const OBSTACLE_VFX = {
     GLOW_PULSE_SPEED: 0.01,    // radians per ms for danger pulse
     GLOW_MIN_ALPHA: 0.4,       // minimum glow brightness
     GLOW_RANGE: 0.4,           // alpha oscillation range
     GLOW_PADDING: 3,           // px padding around obstacle for glow rect
     LETTER_FONT_SIZE: 26,      // px
     LETTER_OUTLINE_WIDTH: 3,   // px dark outline
   };
   ```
  </action>
  <verify>
Open game. Obstacles should:
1. NOT have blurry shadow glow (no shadowBlur visual artifact)
2. Show a pulsing glow when they enter left 30% of screen (danger zone)
3. Display letters at 26px bold with visible dark outline readable at any speed
4. No performance regression (check DevTools Performance tab — no excessive paint time)
Run: `grep -n "shadowBlur" src/entities/obstacle.js` — should return nothing
  </verify>
  <done>
- Zero shadowBlur calls in obstacle.js
- Obstacles pulse with screen-blend glow when in danger zone (left 30% of canvas)
- Letters are 26px bold with 3px dark outline, readable at maximum speed
- globalCompositeOperation='lighter' used for glow effect
  </done>
</task>

</tasks>

<verification>
1. `grep -c "shadowBlur" src/entities/obstacle.js` — returns 0
2. `grep "globalCompositeOperation" src/entities/obstacle.js` — contains 'lighter'
3. `grep "strokeText" src/entities/obstacle.js` — dark outline present
4. `grep "bold 26px" src/entities/obstacle.js` — font size correct
5. Visual test: obstacles glow/pulse only when entering danger zone
</verification>

<success_criteria>
- Obstacle letters pulse/glow when entering the danger zone (VIS-03)
- Glow uses screen-blend (globalCompositeOperation='lighter') not shadowBlur (VIS-04)
- Letters are 26px bold with dark outline, readable at max speed (VIS-05)
- No performance regression compared to current shadowBlur approach
</success_criteria>

<output>
After completion, create `.planning/phases/15-visual-polish/15-02-SUMMARY.md`
</output>
