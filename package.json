{
  "name": "letter-runner",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "size-check": "vite build 2>&1 | grep -E '(kB|MB)'"
  },
  "devDependencies": {
    "vite": "^6.3.0"
  }
}
